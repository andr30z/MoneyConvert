import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex:1,
        maxWidth:120,
    },
    picker:{
        color:'#fff',
        textDecorationColor:'#fff',
        borderWidth:1,
        marginLeft:10
    }
});
